# 함수 
# 특정 역할을 하는 하나의 덩어리 
# 특정 인자를 전달하였을 때, 특정 값을 받는 것이 함수의 기본이다. 

def open_acc(): # 정의 방법 
    print("새로운 계좌가 생성되었습니다.") # 내부에 명령문을 지정하여, 호출시 어떤 일을 하는지 지정해둔다. 

open_acc() # 실제로 호출하지 않는 한 함수는 실행되지 않는다. 